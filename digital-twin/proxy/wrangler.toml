# Digital Twin proxy — Cloudflare Worker config
# Deploy:  npx wrangler deploy          (from digital-twin/proxy/)
# Secret:  npx wrangler secret put GEMINI_API_KEY
# Local:   cp .dev.vars.example .dev.vars && npx wrangler dev

name = "nk-digital-twin"
main = "worker.js"
compatibility_date = "2026-07-01"
workers_dev = true
preview_urls = false

# Bundle twin-context.md (one level up) as a text module at deploy time.
# The file is gitignored — run the nk-twin-context-builder skill to create it.
[[rules]]
type = "Text"
globs = ["**/*.md"]
fallthrough = true

# Native per-colo rate limiting (reliable across isolates)
[[unsafe.bindings]]
name = "RATE_LIMITER"
type = "ratelimit"
namespace_id = "1001"
simple = { limit = 8, period = 60 }

# Much stricter limiter for chat-summary notifications (phone pings)
[[unsafe.bindings]]
name = "SUMMARY_LIMITER"
type = "ratelimit"
namespace_id = "1002"
simple = { limit = 2, period = 60 }

[vars]
# Comma-separated allow-list. Add http://localhost:8000 while developing.
ALLOWED_ORIGINS = "https://nikhilkomakula.github.io"
GEMINI_MODEL = "gemini-3.1-flash-lite"
MAX_OUTPUT_TOKENS = "512"
RATE_LIMIT_PER_MIN = "8"
CACHE_TTL_SECONDS = "3600"

# NTFY_TOPIC (chat-summary notifications) is a SECRET — the repo is
# public, so the topic must never be committed here:
#   npx wrangler secret put NTFY_TOPIC   (use a high-entropy topic name)

# GEMINI_API_KEY is a secret — never put it here.
# npx wrangler secret put GEMINI_API_KEY
